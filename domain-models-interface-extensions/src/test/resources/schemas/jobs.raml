#%RAML 0.8

title: Jobs API
baseUri: http://localhost:8081/v1
version: v1

schemas:
  - jobs.schema: !include jobs.schema
  - job.schema: !include job.schema

/jobs:
    displayName: Jobs
    description: Jobs services
    post:
      body:
        application/json:
          schema: jobs.schema
